import { APP_NAME } from '../../constants/app';

export function Footer() {
  return (
    <footer className="footer">
      <span>{APP_NAME}</span>
      <span>Base del proyecto lista para nuevos modulos.</span>
    </footer>
  );
}
